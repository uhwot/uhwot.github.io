{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "dev server",
            "request": "launch",
            "command": "yarn run devserver",
            "cwd": "${workspaceFolder}"
        }
    ]
}